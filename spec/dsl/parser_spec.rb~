RSpec.describe Tourbillon do
  it "has a version number" do
    expect(Tourbillon::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end

  it 'parse cron file' do
    Tourbillon.parse_cron
  end
end
